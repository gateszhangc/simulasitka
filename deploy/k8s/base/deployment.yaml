apiVersion: apps/v1
kind: Deployment
metadata:
  name: simulasitka
  labels:
    app.kubernetes.io/name: simulasitka
    app.kubernetes.io/part-of: simulasitka
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: simulasitka
      app.kubernetes.io/part-of: simulasitka
  template:
    metadata:
      labels:
        app.kubernetes.io/name: simulasitka
        app.kubernetes.io/part-of: simulasitka
    spec:
      imagePullSecrets:
        - name: dokploy-fleet-ghcr
      containers:
        - name: web
          image: registry.144.91.77.245.sslip.io/simulasitka:bootstrap
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: HOSTNAME
              value: 0.0.0.0
          ports:
            - name: http
              containerPort: 3000
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: /healthz
              port: 3000
            failureThreshold: 30
            periodSeconds: 5
